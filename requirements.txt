# Kaggle GPU images already provide a CUDA-compatible torch + torchvision pair.
# Do not reinstall them here: replacing Kaggle's pair can break CUDA support and
# downloads several gigabytes. For a local machine, install PyTorch first from
# https://pytorch.org/get-started/locally/ and then install this file.

numpy>=1.24,<3
scipy>=1.10,<2
Pillow>=9,<13
scikit-image>=0.20,<1
opencv-python-headless>=4.7,<5
matplotlib>=3.7,<4
tensorboard>=2.12,<3
tqdm>=4.65,<5
